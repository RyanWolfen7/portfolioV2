import React, { useState} from 'react'
import { CarouselContent } from '../../styles/CarouselStyles'

const CardCarousel = props => {

    return (
        <CardCarousel>
        </CardCarousel>
    )
}

export default CardCarousel